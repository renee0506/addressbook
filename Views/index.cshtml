@{
  Layout="Shared/_Layout.cshtml";
}

<h1>Contacts</h1>
 @if(Model.Count > 0){
  @foreach (var person in Model)
  {
    <h2><a href = "/contact/@person.GetId()/info">@person.GetName()</a></h2>
  }
}
else{
  <h2>Address book is currently empty</h2>
}
